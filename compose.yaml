services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=utilisateur-service'
      - 'POSTGRES_PASSWORD=nnr@'
      - 'POSTGRES_USER=nnr'
    ports:
      - '1500:5432'
